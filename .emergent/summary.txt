Project context (merged from prior agent notes; conflict markers removed).

Original direction included: core combat (hitboxes, MoveData, cancels), HUD meters, boss/arena content, gamepad, VFX, tests, and world/tail lore for Legends of Kai-Jax.

The repository evolved into a React + Three.js + Vite web client (apps/web) with canon-first data, validation scripts, optional FastAPI hub backend, and district/adventure flows. A separate engine-agnostic design spec was also discussed for Unreal/Unity; the active shipping path is the web client unless the user pivots.

What exists (high level)
- Game hub and lore content appear in various frontend paths; primary playable client: apps/web.
- Backend (optional): FastAPI + Mongo for hub features per AGENTS.md.

Pending / recurring themes from notes
- AI image flow was partially wired in some forks; may need UI completion if that feature returns.
- Monolithic files and content-in-JSX were noted as refactor candidates where still true.

Credentials: N/A in these notes.

This file intentionally contains no Git conflict marker lines so merges and tooling stay clean.
