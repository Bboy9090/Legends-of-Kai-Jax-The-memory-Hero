# Unreal — Unreal-only direction

**Used only when we switch to Unreal.** This folder overrides primary for Unreal-specific specs (engine, platform, pipeline, Unreal-only systems).

Do not put active Three.js / current-build work here.
