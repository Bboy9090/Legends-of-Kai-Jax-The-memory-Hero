name: Actions Startup Probe

on:
  push:
    branches:
      - feature/fighter-select-v2
  workflow_dispatch:

jobs:
  probe:
    name: Runner Startup Probe
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Confirm runner startup
        run: echo "GitHub Actions runner started successfully"
